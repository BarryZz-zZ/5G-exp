# 5G实验完整报告（频谱分析 + TOA估计 + PSS识别 + 大频偏补偿）

## 1. 实验目的
1. 对接收信号进行频谱分析，观察信号能量在频域的分布。
2. 使用时域互相关方法估计信号到达时间（TOA）。
3. 利用 3 组本地模板信号完成主同步信号（PSS）识别，给出 \(N_{PSS}\)。
4. 针对“含大频偏信号相关峰不明显”的问题，给出可执行解决方案并完成可视化验证。

## 2. 数据与实验条件
数据文件：`data_5G.mat`  
采样率：\(F_s = 7.68\text{ MHz}\)

变量说明：
1. `recv_signal`：正常接收信号，维度 `460800 x 1`（复数）
2. `recv_signal_freq_offset`：含大频偏接收信号，维度 `460800 x 1`（复数）
3. `ref_signal_grid`：本地模板，维度 `256 x 3`（复数），3 列分别对应 3 个 PSS 候选

## 3. 原理说明

### 3.1 频谱分析
采用 Welch 功率谱估计（`pwelch` 或 Python `welch`）：
\[
P_{xx}(f) = \text{Welch}(x[n])
\]
用于观察 `recv_signal` 与 `recv_signal_freq_offset` 的频域特性差异。

### 3.2 TOA 估计（互相关法）
对接收信号 \(r[n]\) 与模板 \(s[n]\) 做互相关：
\[
z[k] = \sum_n r[n]\cdot s^*[n-k]
\]
取 \(|z[k]|\) 最大值对应的时延 \(k_{\max}\) 作为 TOA：
\[
\hat{k}_{TOA} = \arg\max_k |z[k]|
\]
时间估计为：
\[
\hat{t}_{TOA} = \hat{k}_{TOA}/F_s
\]

### 3.3 PSS 识别
分别计算与 3 个模板的相关峰值，峰值最大的模板列号即判决结果。  
本实验程序采用映射：模板列 `1/2/3 -> N_PSS = 0/1/2`。

### 3.4 大频偏导致相关峰异常的原因
若接收信号存在频偏 \(\Delta f\)：
\[
r[n] = s[n-n_0]e^{j2\pi \Delta f n/F_s} + w[n]
\]
模板长度内相位快速旋转会破坏相干累加，导致相关峰弱化、峰位漂移或误检。

### 3.5 进一步讨论的解决方案（已写入程序）
采用“粗同步 + 频偏搜索 + 频偏补偿 + 再相关”的四步法：
1. 先对 `recv_signal_freq_offset` 粗相关，定位粗时延窗口。
2. 在频偏网格上搜索，使局部相干相关度最大：
\[
M(\Delta f)=\left|\sum_n r_{seg}[n]e^{-j2\pi \Delta f n/F_s}s^*[n]\right|
\]
3. 用最优 \(\Delta f\) 对全信号补偿：
\[
r_c[n] = r[n]e^{-j2\pi \hat{\Delta f} n/F_s}
\]
4. 对补偿后信号重新进行 TOA + PSS 判决。

## 4. 程序说明

### 4.1 MATLAB 主程序
文件：`run_5g_experiment.m`

主要模块：
1. 数据加载与参数初始化（`Fs=7.68e6`）
2. 时域波形图与 PSD 图绘制
3. `detect_toa_pss`：完成 TOA 与 PSS 判决
4. `solve_large_cfo`：实现大频偏补偿（粗细网格搜索）
5. 补偿前后相关对比、相位漂移对比可视化
6. 自动保存图像到 `figures_matlab`

### 4.2 Python 配图程序（报告生成）
文件：`generate_report_figures.py`

作用：
1. 用与 MATLAB 一致的方法复现实验流程
2. 自动生成报告图片到 `report_figures`
3. 输出 `results_summary.json` 作为报告数据依据

## 5. 实验结果

### 5.1 正常信号 `recv_signal` 结果
1. 最优模板列号：`2`
2. 判决 `N_PSS`：`1`
3. TOA 时延：`142275` samples
4. TOA 时间：`18525.52 us`
5. 三模板峰值（abs）：`[723.96, 836.45, 730.26]`
6. 三模板峰值指标（peak/median）：`[178.17, 206.07, 180.39]`

### 5.2 含频偏信号 `recv_signal_freq_offset`（补偿前）
1. 最优模板列号：`3`
2. 判决 `N_PSS`：`2`
3. TOA 时延：`296152` samples
4. TOA 时间：`38561.59 us`

### 5.3 大频偏补偿后结果
1. 频偏估计所选模板列号：`3`
2. 估计频偏：`116700 Hz`
3. 补偿后最优模板列号：`1`
4. 补偿后判决 `N_PSS`：`0`
5. 补偿后 TOA 时延：`142888` samples
6. 补偿后 TOA 时间：`18605.34 us`

说明：补偿后 TOA 与正常信号的 TOA（约 142k samples）明显接近，说明大频偏处理能够显著改善同步结果的一致性。

## 6. 可视化结果

### 图1 时域波形
![fig1](report_figures/fig1_time_domain.png)

### 图2 功率谱密度对比
![fig2](report_figures/fig2_psd.png)

### 图3 正常信号三模板相关曲线
![fig3](report_figures/fig3_corr_recv_signal.png)

### 图4 正常信号 PSS 判决特征（峰值/峰值指标）
![fig4](report_figures/fig4_pss_features_recv_signal.png)

### 图5 含频偏信号补偿前三模板相关曲线
![fig5](report_figures/fig5_corr_freq_offset_raw.png)

### 图6 频偏细搜索指标曲线
![fig6](report_figures/fig6_cfo_search.png)

### 图7 最优模板补偿前后相关对比
![fig7](report_figures/fig7_corr_before_after_comp.png)

### 图8 匹配窗口内相位漂移补偿前后对比
![fig8](report_figures/fig8_phase_before_after_comp.png)

### 图9 含频偏信号补偿后三模板相关曲线
![fig9](report_figures/fig9_corr_freq_offset_comp.png)

## 7. 进一步讨论结论（对应PPT最后一页）
问题：`recv_signal_freq_offset` 相关结果无明显峰怎么办？

结论：
1. 根因是频偏导致模板长度内相位快速旋转，相干相关被破坏。
2. 工程上应先做频偏估计与补偿，再做 TOA/PSS 判决。
3. 本实验中使用“粗同步 + 频偏网格搜索 + 补偿 + 再相关”后，TOA 从异常位置拉回到与正常信号接近的位置，证明方案有效。

## 8. 复现步骤
1. 在 MATLAB 中运行：
   `run_5g_experiment.m`
2. 在终端运行（生成报告图片与结果 JSON）：
   `python generate_report_figures.py`
3. 查看报告：
   `5G实验完整报告.md`
